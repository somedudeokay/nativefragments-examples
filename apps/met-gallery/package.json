{
  "name": "nativefragments-demo-met-gallery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "dev:node": "node scripts/serve-node.mjs",
    "deploy": "npx wrangler deploy",
    "check": "node --check worker.js && find site public/app scripts test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "dependencies": {
    "@nativefragments/core": "^0.5.0"
  }
}
