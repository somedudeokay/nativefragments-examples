{
  "name": "@nativefragments/demo-theme-switcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "check": "node --check worker.js && find site public/app test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "dependencies": {
    "@nativefragments/core": "^0.5.0"
  },
  "devDependencies": {
    "@web/test-runner": "^0.20.2"
  }
}
