{
  "name": "@nativefragments-demo/nested-routes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler@latest dev",
    "deploy": "npx wrangler@latest deploy",
    "check": "node --check worker.js && find site public/app test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@nativefragments/core": "^0.4.1"
  },
  "devDependencies": {
    "@web/test-runner": "^0.20.0"
  }
}
